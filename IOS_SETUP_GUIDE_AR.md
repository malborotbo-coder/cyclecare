# دليل إعداد تطبيق iOS - Cycle Care

## 🎉 مبروك! تم إنشاء مشروع Xcode بنجاح!

تم تحويل تطبيق الويب الخاص بك إلى تطبيق iOS جاهز للتشغيل على Xcode.

**موقع التطبيق:** [cyclecatrtec.com](https://cyclecatrtec.com)

---

## 📂 موقع ملفات المشروع

مشروع iOS الخاص بك موجود في:
```
ios/App/
├── App.xcworkspace    ← افتح هذا الملف في Xcode
├── App.xcodeproj/
└── App/
    ├── public/        ← ملفات التطبيق (HTML, CSS, JS)
    └── Assets.xcassets/  ← الأيقونات والصور
```

---

## 🚀 الخطوات التالية

### 1️⃣ فتح المشروع في Xcode

**على جهاز Mac:**

```bash
# الطريقة الأولى: من Terminal
cd ios/App
open App.xcworkspace

# الطريقة الثانية: من Finder
# انتقل إلى مجلد ios/App وافتح ملف App.xcworkspace
```

⚠️ **مهم جداً:** افتح ملف `App.xcworkspace` وليس `App.xcodeproj`

---

### 2️⃣ تشغيل التطبيق على المحاكي (Simulator)

1. افتح Xcode
2. اختر جهاز المحاكي من القائمة العلوية (مثل: iPhone 15 Pro)
3. اضغط على زر ▶️ (Run) أو اضغط `Cmd + R`
4. سيفتح المحاكي ويشغل تطبيقك

---

### 3️⃣ تشغيل التطبيق على جهازك الحقيقي (iPhone/iPad)

**المتطلبات:**
- حساب Apple Developer (مجاني للتطوير، 99$ سنوياً للنشر)
- كيبل USB لتوصيل الجهاز بالحاسوب

**الخطوات:**

1. **وصّل جهازك بالماك**
   - وصّل iPhone/iPad بكيبل USB
   - افتح الجهاز واضغط "Trust This Computer"

2. **في Xcode:**
   - اختر اسم جهازك من القائمة العلوية بدلاً من المحاكي
   - اذهب إلى `Signing & Capabilities`
   - حدد Apple ID الخاص بك في "Team"
   - اضغط Run

3. **على الجهاز:**
   - اذهب إلى: Settings > General > VPN & Device Management
   - اضغط على اسم المطور (Apple ID الخاص بك)
   - اضغط "Trust"
   - ارجع للتطبيق وشغّله

---

## 🎨 تخصيص الأيقونات والشاشة الافتتاحية

### تغيير الأيقونة (App Icon):

1. افتح Xcode
2. في المتصفح الجانبي، اختر `Assets.xcassets`
3. اضغط على `AppIcon`
4. اسحب صورك بالأحجام المطلوبة (يفضل 1024x1024 PNG)

**أو استخدم أداة توليد:**
- استخدم موقع https://appicon.co لتوليد جميع الأحجام تلقائياً
- ارفع صورة 1024x1024
- حمّل ملف ZIP
- استخرج الملفات واسحبها إلى AppIcon في Xcode

### تغيير الشاشة الافتتاحية (Splash Screen):

1. في Xcode، اختر `Assets.xcassets` > `Splash`
2. استبدل صورة `splash-2732x2732.png`
3. أو عدّل `LaunchScreen.storyboard` لتصميم مخصص

---

## ⚙️ تعديل معلومات التطبيق

### تغيير اسم التطبيق:

1. افتح `ios/App/App/Info.plist`
2. ابحث عن `CFBundleDisplayName`
3. غيّر القيمة إلى اسم تطبيقك (مثل: "خدمة الدراجات")

### تغيير Bundle Identifier (App ID):

1. في Xcode، اختر المشروع من الأعلى
2. اذهب إلى تبويب "General"
3. Bundle Identifier الحالي: `com.cyclecatrtec.app`
   - يمكنك تغييره إلى معرّف مخصص إذا أردت
   - مثال: `com.yourcompany.cyclecare`
   - يجب أن يكون فريداً على App Store

---

## 🔄 تحديث التطبيق بعد تعديل الكود

⚠️ **مهم جداً:** لازم تستخدم `--base=./` عند البناء لتطبيقات iOS!

عند تعديل ملفات React/JavaScript، عندك 3 خيارات:

### الخيار 1: استخدام السكريبت الجاهز (الأسهل) ✨
```bash
./build-ios.sh
```

### الخيار 2: أوامر منفصلة
```bash
# 1. ابنِ التطبيق من جديد (لاحظ --base=./)
npx vite build --base=./

# 2. زامن الملفات مع iOS
npx cap sync ios

# 3. ارجع لـ Xcode واضغط Run
```

### الخيار 3: أمر واحد
```bash
npx vite build --base=./ && npx cap sync ios
```

⚠️ **تنبيه مهم جداً:** 
- ❌ **لا تستخدم** `npm run build` العادي أبداً!
- ✅ **استخدم دائماً** `./build-ios.sh` أو `npx vite build --base=./`
- السبب: `npm run build` لن يضيف `--base=./` وسيؤدي لشاشة بيضاء في التطبيق!

---

## 📱 نشر التطبيق على App Store

### المتطلبات:
- حساب Apple Developer ($99/سنة)
- شهادات التوقيع والملفات التعريفية
- معلومات التطبيق وصور للعرض

### الخطوات المختصرة:

1. **التسجيل في Apple Developer:**
   - اذهب إلى https://developer.apple.com
   - سجّل واشترك ($99/سنة)

2. **إنشاء App في App Store Connect:**
   - اذهب إلى https://appstoreconnect.apple.com
   - اضغط "My Apps" > "+" > "New App"
   - أدخل معلومات التطبيق

3. **في Xcode:**
   - اختر "Any iOS Device (arm64)" من القائمة العلوية
   - اذهب إلى: Product > Archive
   - انتظر حتى يكتمل الأرشفة
   - اضغط "Distribute App"
   - اتبع التعليمات لرفع التطبيق

4. **في App Store Connect:**
   - أدخل وصف التطبيق وصور الشاشات
   - حدد السعر (مجاني أو مدفوع)
   - قدّم التطبيق للمراجعة

---

## ⚡ نصائح مهمة

### 🔐 الخصوصية والأذونات:

إذا كان تطبيقك يستخدم:
- **الكاميرا:** أضف `NSCameraUsageDescription` في Info.plist
- **الموقع:** أضف `NSLocationWhenInUseUsageDescription`
- **الصور:** أضف `NSPhotoLibraryUsageDescription`

**كيفية الإضافة:**
1. افتح `ios/App/App/Info.plist`
2. اضغط "+" لإضافة سطر جديد
3. اختر الإذن المطلوب
4. أدخل رسالة توضح سبب الطلب (مثل: "نحتاج الكاميرا لتصوير الدراجة")

### 🌐 الاتصال بالسيرفر:

إذا كان التطبيق يتصل بـ API خارجي:
- تأكد من أن السيرفر يستخدم HTTPS (مو HTTP)
- أو أضف exception في Info.plist لـ HTTP

### 📦 حجم التطبيق:

- التطبيق الحالي حجمه حوالي 6-8 MB
- يمكن تقليله بحذف الصور الكبيرة أو ضغطها

---

## 🔧 حل المشاكل الشائعة

**📘 للحصول على دليل شامل لحل جميع مشاكل Xcode، راجع ملف:**
**[XCODE_TROUBLESHOOTING_AR.md](XCODE_TROUBLESHOOTING_AR.md)**

يحتوي على حلول مفصلة لـ:
- ✅ مشكلة `module 'Cordova' not found`
- ✅ مشكلة AppIcon غير موجودة
- ✅ تحذيرات CocoaPods
- ✅ خطوات إعادة تثبيت كاملة

---

### ❌ خطأ: "Code Signing Error"
**الحل:**
1. اذهب إلى `Signing & Capabilities` في Xcode
2. اختر Team الخاص بك
3. فعّل "Automatically manage signing"

### ❌ التطبيق يفتح صفحة بيضاء
**الحل:**
```bash
# تأكد من البناء بـ base path صحيح
npx vite build --base=./
npx cap sync ios
```

### ❌ الأيقونات ما تظهر
**الحل:**
- تأكد من أن الصور بصيغة PNG
- تأكد من الأحجام المطلوبة (1024x1024 للأساسي)

### ❌ "Unable to install on device"
**الحل:**
- تأكد من Trust الجهاز على الماك
- تأكد من Trust المطور على الجهاز (Settings > General > VPN & Device Management)

---

## 📚 موارد إضافية

- [دليل Capacitor الرسمي](https://capacitorjs.com/docs)
- [دليل Apple للنشر](https://developer.apple.com/app-store/submissions/)
- [Xcode Documentation](https://developer.apple.com/documentation/xcode)

---

## 🎯 الخطوات التالية المقترحة:

1. ✅ جرّب التطبيق على المحاكي
2. ✅ غيّر الأيقونة والشاشة الافتتاحية
3. ✅ جرّب على جهازك الحقيقي
4. ✅ اضبط الأذونات إذا لزم
5. ✅ سجّل في Apple Developer
6. ✅ انشر على App Store!

---

## ❓ أسئلة شائعة

**س: هل أحتاج جهاز Mac لنشر التطبيق؟**
ج: نعم، لازم Mac لفتح Xcode وبناء تطبيقات iOS.

**س: كم تستغرق مراجعة Apple؟**
ج: عادةً 24-48 ساعة، لكن قد تصل لأسبوع.

**س: هل يمكن تحديث التطبيق بدون إعادة النشر؟**
ج: التحديثات البسيطة (HTML/CSS/JS) يمكن عملها عبر السيرفر، لكن التحديثات الكبيرة تحتاج إعادة نشر.

**س: التطبيق يعمل على الويب لكن ما يشتغل على iOS؟**
ج: تحقق من Console في Xcode (View > Debug Area > Activate Console) للأخطاء.

---

✨ **مبروك! تطبيقك جاهز للانطلاق!** ✨

في حال واجهتك أي مشكلة، راجع [Capacitor Troubleshooting](https://capacitorjs.com/docs/basics/workflow#debugging)
