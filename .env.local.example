# Cycle Care - Environment Variables

# ⚠️ NATIVE AUTH BYPASS (للتطوير فقط / Development Only)
# لتجربة التطبيق على iOS بدون تسجيل دخول حقيقي
# To test the app on iOS without real authentication
# Default: false (أمان أولاً / security first)
# Set to 'true' ONLY for local iOS testing (للتجربة المحلية فقط)
# ⚠️ NEVER deploy to App Store with this enabled!
# VITE_NATIVE_AUTH_BYPASS=false

# For local iOS testing, uncomment and set to true:
# VITE_NATIVE_AUTH_BYPASS=true

# ملاحظة: يجب تعطيل هذا قبل النشر على App Store
# Note: Must disable this before publishing to App Store
